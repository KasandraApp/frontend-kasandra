# Frontend environment settings for Kasandra integration
# Set the backend API base URL used by frontend auth and data services.
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Optional: Backend endpoint for Google OAuth if frontend will link directly.
VITE_GOOGLE_AUTH_URL=http://localhost:3000/api/v1/auth/google
